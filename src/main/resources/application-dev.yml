#====================================================
# 测试环境配置
#
# 完整文档：
#====================================================

spring:
  # 默认时间格式
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  #  cloud:
  #    inetutils:
  #      default-ip-address: localhost

# 微服务注册
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://admin:root@localhost:10000/eureka/
#    healthcheck:
#      enabled: true
#  instance:
#    hostname: ${spring.cloud.inetutils.default-ip-address}
#    ip-address: ${spring.cloud.inetutils.default-ip-address}
#    prefer-ip-address: true
#    instance-id: ${spring.cloud.inetutils.default-ip-address}:${server.port}

# 停止服务
endpoints:
  shutdown:
    #开启shutdown端点
    enabled: true
    path: stop
management:
  security:
    #是否启用安全
    enabled: true
  port: 11000
  address: 127.0.0.1

server:
  port: 11000
  tomcat:
    uri-encoding: utf-8
